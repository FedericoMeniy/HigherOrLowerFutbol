# --- Conexi�n a Base de Datos (Inyectada por Render) ---
# Usamos variables de entorno para que sea seguro y flexible
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

# --- JPA / Hibernate ---
# Importante para que MySQL funcione bien en la nube
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
# "update" permite que Hibernate cree/actualice las tablas autom�ticamente si no existen
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# --- Servidor ---
# Render nos asignar� un puerto din�mico, Spring debe leerlo
server.port=${PORT:8080}

# --- Configuraci�n Personalizada ---
# API Sports (F�tbol)
api.url=https://v3.football.api-sports.io
api.key=${API_FOOTBALL_KEY}
api.host=v3.football.api-sports.io

# JWT (Seguridad)
jwt.secret=${JWT_SECRET}